const posts = [
    'this is the first post', 
    'this is some other post', 
    'this is a post of your grandma made', 
    'testinggg', 
    'look at you using array methods!'
]

/**
 * We got some practice we gotta do!
 * using your new iteration skills 
 * iterate through each of the items in the array and have them display on the DOM
 * After completing the problem comment it out and go to the next problem
 */

//  1. Using the for ... of loop

// const mainbox = document.getElementById('main'); 

//  for (const item of posts) {
//      const newPost = document.createElement('p'); 
//      newPost.textContent = item; 
//      mainbox.appendChild(newPost)
//  }

//  2. Using the .forEach() method display the posts

// 3. Using .map() change each item, and then display it on the DOM again using .forEach()

// 4. Using the .find() method only display the post your grandma made

// 5. Using the reduce() method concatinate all the post into a monster post

// 6. Using the .filter() delete any posts your grandma made from the feed.
